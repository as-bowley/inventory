extends layout

block content

  h1 Size: #{size.size}

  div(style='margin-left:20px;margin-top:20px')

    h4 Items

    dl
      each item in size_items
        dt
          a(href=item.url) #{item.name}
        dd #{item.description}

      else
        p This size has no items