extends layout

block content

  h1 Team: #{team.name}

  div(style='margin-left:20px;margin-top:20px')

    h4 Items

    dl
      each item in team_items
        dt
          a(href=item.url) #{item.name}
        dd #{item.description}

      else
        p This team has no items